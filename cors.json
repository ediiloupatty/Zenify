{
  "rules": [
    {
      "allowed": {
        "origins": [
          "https://www.zenify.cc",
          "https://zenify.cc",
          "http://localhost:3000"
        ],
        "methods": ["GET", "PUT", "HEAD"],
        "headers": ["content-type"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
